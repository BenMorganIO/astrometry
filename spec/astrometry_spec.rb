require 'spec_helper'

describe Astrometry do
  it 'has a version number' do
    expect(Astrometry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
